# RAG Pipeline Configuration Skill

## Purpose
Guidelines for chunking, embedding, indexing, and re-ranking documents in RAG systems.

## Best Practices
- Implement structure-aware semantic chunking.
- Use hybrid search matching (dense + sparse).
- Enforce cross-encoder re-ranking for query precision.\n