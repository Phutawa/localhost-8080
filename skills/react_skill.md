# React Component Development Skill

## Purpose
Guidelines and reusable snippets for creating clean, performance-optimized, and accessible React components.

## Best Practices
- Keep components modular (Atomic Design principles).
- Strictly enforce accessibility (ARIA attributes, keyboard navigation).
- Optimize re-renders using standard Hooks (`useMemo`, `useCallback`, `React.memo`).\n