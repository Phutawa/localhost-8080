# Git Version Control Skill

## Purpose
Guidelines for standard Git operations, branching strategies, and commit messages.

## Best Practices
- Enforce clean branch naming (e.g., `feature/`, `bugfix/`, `hotfix/`).
- Write structured commits: `<type>(<scope>): <subject>` (Conventional Commits).
- Pull updates before branching; push commits to isolated remote branches only.
